language: rust

sudo: true

rust:
    - "1.36.0"
    - "1.37.0"
    - "1.38.0"
    - "stable"
    - "beta"
    - "nightly"

matrix:
    allow_failure:
        - rust: "nightly"

before_script:
    - sudo apt-get update -qq
    - sudo apt-get install -y valgrind python3

script:
    - make test
